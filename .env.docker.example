# Copy this file to .env when deploying with Docker Compose.
# Example (PowerShell): Copy-Item .env.docker.example .env

JWT_SECRET=replace_with_a_long_random_secret
# Set this to your deployed frontend/backend URL when needed.
# For local compose usage, http://localhost:3000 is fine.
CORS_ORIGIN=http://localhost:3000,https://groupd.stu.researchatmun.ca
